apiVersion: v1
kind: Service
metadata:
  name: hello-clusterip-svc
  labels:
    app: hello-app
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: hello-app
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}